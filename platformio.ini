[platformio]
extra_configs = platformio_override.ini

[env:heltec_wifi_lora_32_V3]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = heltec_wifi_lora_32_V3
framework = arduino
monitor_speed = 115200
; --- Uncomment and replace with the actual port of your Heltec V3 ---
; upload_port = /dev/cu.usbserial-XXXXX
; monitor_port = /dev/cu.usbserial-XXXXX
; Use a larger partition table to accommodate both Wi-Fi and BLE Provisioning stacks
board_build.partitions = huge_app.csv

; Force libraries to be recompiled from source, ignoring pre-compiled archives.
; This helps ensure build flags are always applied correctly.
lib_archive = false
; Use debug build type to prevent NDEBUG from being defined, which can
; disable Serial output within libraries like RadioLib.
build_type = release
; Use a specific, stable version of RadioLib from the PlatformIO registry.
; This is more reliable than using the latest commit from GitHub.
lib_deps =
  jgromes/RadioLib@^7.6.0
  olikraus/U8g2@^2.35.30
  knolleary/PubSubClient@^2.8
; Use build_flags to apply flags to the build environment. These flags
; are specific to RadioLib and enable its debug/verbose output.


[env:heltec_wifi_lora_32_V3_debug]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = heltec_wifi_lora_32_V3
framework = arduino
monitor_speed = 115200
; --- Port configuration moved to platformio_override.ini ---
; upload_port = /dev/cu.usbserial-0001
; monitor_port = /dev/cu.usbserial-0001
; Use a larger partition table to accommodate both Wi-Fi and BLE Provisioning stacks
board_build.partitions = huge_app.csv

; Force libraries to be recompiled from source, ignoring pre-compiled archives.
; This helps ensure build flags are always applied correctly.
lib_archive = false
; Use debug build type to prevent NDEBUG from being defined, which can
; disable Serial output within libraries like RadioLib.
build_type = debug
; Use a specific, stable version of RadioLib from the PlatformIO registry.
; This is more reliable than using the latest commit from GitHub.
lib_deps =
  jgromes/RadioLib@^7.6.0
  olikraus/U8g2@^2.35.30
  knolleary/PubSubClient@^2.8
; Use build_flags to apply flags to the build environment. These flags
; are specific to RadioLib and enable its debug/verbose output.

[env:lilygo_t3_v16]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = ttgo-lora32-v21
framework = arduino
monitor_speed = 115200
; --- Uncomment and replace with the actual port of your LilyGo T3 ---
; upload_port = /dev/cu.usbserial-YYYYY
; monitor_port = /dev/cu.usbserial-YYYYY
board_build.partitions = huge_app.csv
lib_archive = false
build_type = release
; Pass a global define to tell our code we are compiling for the LilyGO
build_flags =
  -D LILYGO_T3_V16
lib_deps =
  jgromes/RadioLib@^7.6.0
  olikraus/U8g2@^2.35.30
  knolleary/PubSubClient@^2.8

[env:lilygo_t3_v16_debug]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = ttgo-lora32-v21
framework = arduino
monitor_speed = 115200
; --- Port configuration moved to platformio_override.ini ---
; upload_port = /dev/cu.usbserial-59680262361
; monitor_port = /dev/cu.usbserial-59680262361
board_build.partitions = huge_app.csv
lib_archive = false
build_type = debug
build_flags =
  -D LILYGO_T3_V16
  -Os
lib_deps =
  jgromes/RadioLib@^7.6.0
  olikraus/U8g2@^2.35.30
  knolleary/PubSubClient@^2.8
